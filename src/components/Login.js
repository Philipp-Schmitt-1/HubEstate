import React from 'react';

function Login() {
    return (
        <div>
            <h1>Login to HubEstate</h1>
            {/* Include a login form here. On submission, connect to  backend for authentication */}
        </div>
    );
}

export default Login;
