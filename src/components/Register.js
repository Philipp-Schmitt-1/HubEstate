import React from 'react';

function Register() {
    return (
        <div>
            <h1>Register for HubEstate</h1>
            {/* Include a registration form here. On submission, connect to  backend to create a new user */}
        </div>
    );
}

export default Register;
